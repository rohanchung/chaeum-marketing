# 로한 마케팅 작업 규칙

1. 작업 시작 시 `docs/HANDOFF-CURRENT.md`와 연결된 최신 제작방 핸드오프를 읽는다. 오래된 문서의 미완료 선언보다 현재 코드·DB와 최신 인계를 우선한다.
2. 로한은 기획자다. 작은 범위를 빠르게 구현하고 내부 JS/DB 검증 후 피드백을 받는다. 불필요한 브라우저 창·화면 트리 반복 검증을 피한다.
3. 사용자가 GitHub Desktop으로 커밋·푸시한다. 명시적 새 요청 없이 에이전트가 커밋·푸시하지 않는다. 업데이트 완료 답변에는 복사 가능한 Summary와 Description을 반드시 제공한다.
4. 완료한 기능 업데이트마다 `web/src/lib/update-history.ts`에 날짜·제목·변경점·제작방을 최신순으로 추가한다. 로그에는 실제 완료한 변경만 기록하고 사용자 입력·개인정보·비밀값은 넣지 않는다. 과거 이력을 덮어쓰지 않는다.
5. 각 업데이트의 결정·검증·제한은 `docs/update-YYYYMMDD-주제.md`에 남긴다. 제작방 종료 때 새 핸드오프를 작성하고 `docs/HANDOFF-CURRENT.md`의 최신 링크를 바꾼다. 이전 제작방 기록은 보존한다.
6. 허용된 Supabase 프로젝트는 `nwxbfmycimrxaovemela`뿐이다. 로한북·가계부 DB는 접근·수정하지 않는다. 운영 데이터 보존, RLS 및 원자적 저장을 지킨다. 검증 데이터는 롤백한다.
7. 초기 DB 스키마 일부가 수동 적용되어 migration history와 다르다. 무조건적인 db push나 초기 migration 재실행을 금한다. 신규 migration은 원격 적용 이력과 로컬 파일명을 맞춘다.
8. UI는 컴팩트한 채널 중심 가로 날짜 운영시트를 유지한다. 대시보드를 임의로 재설계하지 않는다. 비용 중복 합산 금지, 순위 비합산, 미입력과 0 구분 등의 계약은 최신 핸드오프를 따른다.
9. `web/AGENTS.md`의 Next.js 문서 확인 규칙도 적용한다. 테스트·빌드 통과와 실제 검증하지 않은 범위를 구분해서 보고한다.
